export const height = 600;
export const width = 800;

export const ballSpeed = 10;
export const playerSpeed = 10;

export const ballRadius = 10;

export const playerOffset = 20;
export const playerWidth = 15;
export const playerHeight = 40;

export const initialPositionY = height / 2 - playerHeight / 2;
